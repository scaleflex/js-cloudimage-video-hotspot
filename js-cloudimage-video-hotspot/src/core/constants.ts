/** Exit animation duration in milliseconds */
export const EXIT_ANIMATION_MS = 300;

/** Delay after seek before opening hotspot popover */
export const SEEK_SETTLE_MS = 100;

/** Controls idle timeout before auto-hide */
export const CONTROLS_IDLE_MS = 3000;

/** Delay before hiding a popover on mouseleave (hover mode) */
export const POPOVER_HIDE_DELAY_MS = 200;

/** Keyboard seek step in seconds */
export const KEYBOARD_SEEK_STEP_S = 5;

/** Keyboard volume adjustment step */
export const KEYBOARD_VOLUME_STEP = 0.1;

/** Time threshold for hotspot navigation (seconds) */
export const HOTSPOT_TIME_THRESHOLD_S = 0.1;

/** YouTube time update polling interval */
export const YT_TIME_UPDATE_INTERVAL_MS = 250;

/** Maximum HLS error recovery retries */
export const HLS_MAX_RETRIES = 5;

/** Initial HLS retry delay in milliseconds */
export const HLS_INITIAL_RETRY_MS = 1000;
